# The twitter scraping code goes here


def gettweet(lat, lon, rad):
    """gets the raw text from twitter
    @:param
    lat: type -
    """
    return "Test Tweet from latitude = "+str(lat)+" and longitude = "+str(lon)+" with half side miles = "+str(rad)

